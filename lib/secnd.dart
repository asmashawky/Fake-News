import 'package:flutter/material.dart';

class secnd extends StatelessWidget {
  const secnd({Key? key}) : super(key: key);
  static const RouteName = 'secnd';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
